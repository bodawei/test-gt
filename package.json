{
  "name": "test-gt",
  "version": "1.0.0",
  "description": "Express server with version and health endpoints",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
